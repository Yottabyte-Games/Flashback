using UnityEngine;

namespace _Scripts.Vehicle.UI
{
    public class CursorHideScript : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            Cursor.visible = false;
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
